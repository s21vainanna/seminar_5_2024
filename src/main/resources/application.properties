spring.application.name=demo_eclipse
spring.datasource.url=jdbc:mysql://localhost:3306/Groceries?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=trakikaki32
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.ddl-auto=create-drop


# Hibernate Configuration
#spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
#spring.jpa.hibernate.ddl-auto=update   # Automatically updates the schema if necessary (use with caution in production)

# Optional Hibernate Properties
# spring.jpa.show-sql=true                # Shows the SQL queries being executed
# spring.jpa.properties.hibernate.format_sql=true  # Formats the SQL output for readability

# Spring Boot Data Initialization (optional)
# spring.sql.init.mode=always
# spring.sql.init.data-locations=classpath:/data.sql   # Optional: If you want to pre-populate your DB with data